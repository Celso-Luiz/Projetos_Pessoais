var num1 = inserto.onclick
var operador = operador.onclick
var num2 = inserto.onclick
Var soma = 0
Funcion operacao(num1, num2)
    If (operador === +) {
            soma = num1 + num2;
        }Eles If ( operador === -){
            Soma = num1 - num2;
